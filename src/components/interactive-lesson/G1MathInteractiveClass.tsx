"use client";

import { CinemaProofPlayer } from "@/src/components/cinematic-lesson/CinemaProofPlayer";

/**
 * Canonical G1 Math interactive route shell.
 * Approach C cinematic 3D proof — not a CSS quiz widget.
 */
export function G1MathInteractiveClass() {
  return <CinemaProofPlayer />;
}
