/**
 * Bootstrap entry for HumanEngine — Sara & Ali only.
 * Re-exports quality-gated session start.
 */
export {
  bootstrapTeacher,
  startTeacherSession,
  probeTeacherQuality,
  type TeacherId,
} from "./bootstrap";
